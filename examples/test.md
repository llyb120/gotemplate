# test

## sql1
```sql
select * from table1
where 
1 = 1
AND id = 1, --# val x when true 
and b in ('') --# each arr by ''
and c = 3 --# abc?
and d = 4 --# a by 4

--# use foo.sql3

我是分割线

--# use sql2 as u2

before hook

--# hook u2.a
--# if 1 > 0 
我是替换的slot
--# end
--# end

    
```

